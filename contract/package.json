{
  "name": "main",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "yarn build:as",
    "build:as": "yarn asb ./assembly/main.ts",
    "clean": "rm -rf ./out && (cd contracts/rust && cargo clean)",
    "test": "yarn test:unit:as && yarn test:unit:rs",
    "test:unit:as": "yarn asp --verbose --nologo -c ../contract/assembly/as-pect.config.js -f unit.spec"
  },
  "dependencies": {
    "asbuild": "^0.0.11",
    "near-sdk-as": "2.2.4"
  },
  "devDependencies": {
    "shelljs": "^0.8.4"
  }
}
